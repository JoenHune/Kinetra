# ─────────────────────────────────────────────────────────────────
# Local overrides — this file is committed as a TEMPLATE.
#
# Copy to  config.local.yaml  and fill in your secrets:
#   cp config.local.yaml.example config.local.yaml
#
# config.local.yaml is git-ignored and will NEVER be pushed.
# Any key defined here overrides the same key in config.yaml.
# ─────────────────────────────────────────────────────────────────

llm:
  provider: "deepseek"                # openai | anthropic | deepseek | ollama | custom
  model: "deepseek-chat"
  api_base: ""                        # Leave empty for provider default
  api_key: "sk-xxxxxxxxxxxxxxxx"      # ← YOUR secret key here
